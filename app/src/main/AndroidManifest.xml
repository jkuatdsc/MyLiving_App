<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsc.myliving">

    <application
        android:name=".MyLivingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLiving">
        <activity
            android:name=".presentation.intro.IntroActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.entry.EntryActivity"
            android:exported="true"
            android:theme="@style/Theme.MyLiving.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.main.MainActivity" />
    </application>

</manifest>